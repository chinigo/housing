# Python environment
make install_python
layout python
export PYTHON_MINOR_VERSION=$(python --version | sed -E 's/Python ([0-9]+)\.([0-9]+)\.([0-9]+)/\1.\2/')


# Airflow
export AIRFLOW_DB_DATABASE=airflow
export AIRFLOW_DB_HOST=localhost
export AIRFLOW_DB_PASSWORD=mongoLi4n.moNeTary
export AIRFLOW_DB_PORT=5432
export AIRFLOW_DB_USERNAME=airflow
export AIRFLOW_HOME=./airflow
export AIRFLOW_VERSION=2.7.2
export AIRFLOW_WEB_USERNAME=airflow
export AIRFLOW_WEB_PASSWORD=airflow
export AIRFLOW__DATABASE__SQL_ALCHEMY_CONN="postgresql+psycopg2://${AIRFLOW_DB_USERNAME}:${AIRFLOW_DB_PASSWORD}@${AIRFLOW_DB_HOST}:${AIRFLOW_DB_PORT}/${AIRFLOW_DB_DATABASE}"

# Postgis
export HOUSING_DB_DATABASE=housing
export HOUSING_DB_HOST=localhost
export HOUSING_DB_PASSWORD=sto2pes#friedriCh
export HOUSING_DB_PORT=5432
export HOUSING_DB_USERNAME=housing
